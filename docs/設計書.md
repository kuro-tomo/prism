# 擬似フィーラ 設計書

**バージョン：** 0.1（骨格）  
**最終更新：** 2026-05-26  
**ステータス：** 設計諮問待ち

---

## 1. アーキテクチャ概要

```
┌─────────────────────────────────────────────┐
│  Frontend（Next.js）                          │
│  社長が課題入力 → 結果表示                     │
└───────────────┬─────────────────────────────┘
                │ HTTP
┌───────────────▼─────────────────────────────┐
│  API Server（FastAPI）                        │
│  POST /debate  GET /debates                  │
└───────────────┬─────────────────────────────┘
                │
┌───────────────▼─────────────────────────────┐
│  Debate Engine（Python）                      │
│  agents.py / debate.py / memory.py           │
└──────────┬────────────────┬─────────────────┘
           │                │
┌──────────▼──────┐  ┌──────▼──────────────────┐
│  Anthropic API  │  │  Supabase               │
│  Claude Opus    │  │  debates テーブル        │
│  Claude Haiku   │  │  company_context テーブル│
└─────────────────┘  └─────────────────────────┘
```

---

## 2. モジュール構成

| ファイル | 責務 |
|---------|------|
| `engine/agents.py` | エージェントペルソナ定義 |
| `engine/debate.py` | 3ラウンド熟議ループ |
| `engine/memory.py` | Supabase読み書き・文脈生成 |
| `api/main.py` | FastAPI エンドポイント |
| `frontend/` | Next.js UI（未着手） |

---

## 3. データモデル（暫定）

```sql
-- 議論ログ
CREATE TABLE debates (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    created_at  TIMESTAMP DEFAULT NOW(),
    question    TEXT NOT NULL,
    synthesis   TEXT NOT NULL,
    full_log    JSONB
);

-- 会社コンテキスト
CREATE TABLE company_context (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    key         TEXT NOT NULL,
    value       TEXT NOT NULL,
    updated_at  TIMESTAMP DEFAULT NOW()
);
```

> ※ /advisor 設計諮問後に確定・マイグレーション作成

---

## 4. API契約（暫定）

### POST /debate
```json
// Request
{ "question": "string" }

// Response
{
  "id": "uuid",
  "question": "string",
  "synthesis": "string",
  "round1": { "エージェント名": "回答" },
  "round2": { "エージェント名": "反論" }
}
```

---

## 5. 未決事項

- [ ] 並列API呼び出し（asyncio）の採用可否
- [ ] フロントエンドのフレームワーク確定
- [ ] Supabaseスキーマの最終確定
- [ ] 収束チェックアルゴリズムの設計
