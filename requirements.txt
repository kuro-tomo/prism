# AI — Anthropic（討論エンジン）
anthropic>=0.40.0

# API server（Phase 3）
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.0        # SSE 配信（仕様書 A-004・F-010）
jinja2>=3.1.0               # HTMX テンプレート
python-multipart>=0.0.9     # フォームデータ（Magic Link 送信フォーム）
pyjwt[cryptography]>=2.8.0  # Supabase JWT 検証（S-004）
httpx>=0.27.0               # Voyage AI embedding API（voyage-3）・FastAPI テスト

# Database — Supabase（管理操作・Auth）
supabase>=2.10.0

# Database — asyncpg（高速非同期 PostgreSQL 接続・pgvector クエリ）
asyncpg>=0.29.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.9.0
aiosmtplib>=3.0.0             # メール通知（N-002・Phase 3.5）

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
